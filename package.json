{
  "devDependencies": {
    "cypress": "^12.1.0",
    "cypress-xpath": "^2.0.1"
  },
  "scripts": {
    "regression-test": "cypress run --browser chrome --spec cypress/e2e/1-getting-started/**.js"
  }
}
